#include<stdio.h>
#include<conio.h>
int minutes(int hr, int min)
{
     min+=(hr*60);
     return min;
}
void main()
{ 
    int hr, min;
    printf("\nEnter time in hours and minutes");
    scanf("%d%d",&hr,&min);
    printf(" the total time in minutes is %d", minutes(hr,min));
    getch();
}
